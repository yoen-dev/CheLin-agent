# COLLAB.md（协作说明 —— 提交前请补全真实信息）

> 这是模板，占位内容用 `[]` 标出，提交前记得替换成你们两人真实的分工情况。
> 题目原文：commit 历史要能看出两个人都有实质性提交，不是一个人写完另一个人补空 commit。

## 分工

- **[队员A姓名]**：[比如：负责 Controller/状态机/限流器/对抗测试设计]
- **[队员B姓名]**：[比如：负责 LLM 接入/Prompt设计/前端聊天页面/README]

## 时间投入

- 实际花费时间：[X 小时 / X 天]，其中 Day1 主要做了 [...]，Day2 主要做了 [...]

## 中间分歧与解决方式

- 例：一开始讨论过要不要用 LangChain 之类的 agent 框架，
  后来达成一致意见：约束条件（限流/状态机/白名单）需要强代码控制、
  容易测试和讲清楚，框架反而会让"哪一层在强制约束"变得不透明，
  所以决定不用现成框架，自己写 Controller。
- [请替换成你们真实讨论过的分歧点]

## Commit 记录自查

提交前跑一下，确认两人都有实质性提交（不是空 commit）：

```bash
git log --pretty=format:"%an" | sort | uniq -c
git log --stat
```
